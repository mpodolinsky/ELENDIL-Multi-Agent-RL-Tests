# hemac_env_config.yaml
render_mode: null   # Use "rgb_array" or null depending on whether you want rendering
n_drones: 1
n_observers: 1
n_provisioners: 0
rescuing_targets: false
observer_detection_reward: 20.0
# drone_config:
#   discrete_action_space: true


area_size: [1000, 1000]
max_cycles: 900
min_obstacles: 0
max_obstacles: 0            # No obstacles means infinite comm range for the observer (otherwise obstacles act as relays)

poi_config:
  - speed: 1.0
    dimension: [8, 8]
    spawn_mode: random       # Should create one default POI

# Optional sensor models (commented out in Python version)
# drone_sensor:
#   model: RoundCamera
#   params:
#     sensing_range: 30

# observer_sensor:
#   model: ForwardFacingCamera
#   params:
#     hfov: 0.5235987756    # π/6 radians
#     sensing_range: 100
